# From a clean local clone of DocDamage/bodypartchopperforsprites:

git checkout main
git pull --ff-only

git apply v7-app-core-constants.patch

npm test
npm run serve

# Manual browser check:
# Open http://localhost:5173
# Verify the app loads, the visible brand patches to v7, importing a spritesheet still works,
# and the command/menu buttons still respond.

git add app.js index.html tests/static-smoke-test.mjs docs/V7_RUNTIME_SHELL.md docs/V7_APP_CONSTANTS.md

git commit -m "Import V7 core constants into browser app"
git push
