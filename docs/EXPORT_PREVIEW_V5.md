# Export Preview V5

Use **Tools > Export Preview JSON** before packaging. The preview lists all frame paths, manifest paths, project files, QA reports, and credits files that the active profile intends to export.
